.DS_Store
DerivedData/
build/
*.xcuserstate
xcuserdata/
*.xcworkspace/xcuserdata/
.swiftpm/
Secrets.xcconfig
.omx/

# Historical handoff bundle: keep only its write-ups. The maintained Gateway
# implementation now lives in vance-gateway/ below.
Vance_Coach_Gateway_Handoff_20260802/*
!Vance_Coach_Gateway_Handoff_20260802/*.md

# The realtime Gateway source is versioned, but local credentials and runtime
# telemetry must never enter Git.
vance-gateway/.env
vance-gateway/data/
vance-gateway/node_modules/
