# Vance backend 生产密钥模板
# 实际生产文件在 ECS 的 /opt/prod-config/vance/.env，永远不会提交到 Git
#
# 生成方式（在 ECS 上执行一次）：
#   mkdir -p /opt/prod-config/vance
#   openssl rand -hex 32  # → VANCE_WORKER_SECRET
#   openssl rand -hex 32  # → VANCE_GATEWAY_SECRET
# 然后把真实的 API key 填进去，chmod 600

# 与 sourcerlinda 完全隔离的独立密钥。iOS uses each one on its corresponding
# public path: COACH_SHARED_SECRET -> VANCE_WORKER_SECRET, and
# REALTIME_GATEWAY_SECRET -> VANCE_GATEWAY_SECRET.
VANCE_WORKER_SECRET=
VANCE_GATEWAY_SECRET=

# 第三方 API（与 sourcerlinda 共用账号，但应用层隔离）
DEEPSEEK_API_KEY=
MINIMAX_API_KEY=
KIMI_API_KEY=
